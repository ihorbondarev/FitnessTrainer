<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:FitnessTrainer.MoblieApp.ViewModels"
             x:Class="FitnessTrainer.MoblieApp.Views.RegisterPage">

    <ContentPage.BindingContext>
        <viewModels:RegisterViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" Padding="5">
            <Entry Text="{Binding UserName}" TextColor="Black" PlaceholderColor="DarkGray" Placeholder="Login"></Entry>
            <Entry Text="{Binding Password}" TextColor="Black" Placeholder="Password" PlaceholderColor="DarkGray" IsPassword="True"></Entry>
            <Entry Text="{Binding ConfirmPassword}" TextColor="Black" Placeholder="ConfirmPassword" PlaceholderColor="DarkGray" IsPassword="True"></Entry>
            <Button Command="{Binding RegisterRequest}" Text="Register"></Button>
            <Label Text="{Binding Message}" TextColor="DarkGray" FontSize="24"></Label>
            <Label Text="Already have account? Login in" TextColor="Blue"></Label>
            <Button Text="Login" Clicked="routeToLoginPage"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>